# nrhl-hacknight-twilio

Meteor solution to the hack, with Twilio call from the client to function on the server

```clone
install meteor
npm install
copy sample_settings.json to settings.json with your Twilio account
npm start```
